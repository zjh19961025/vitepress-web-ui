/**
 * HuiSelectLoadStatus 需要的props参数
 */
export interface HuiSelectLoadStatusPropsType {
  /** loading状态 */
  loading: boolean;
  /** 显示options数据的length */
  length: number;
  /** 剩余的条数 */
  leave: number;
}
